We must ensure that NoRPM is used to address security concerns

We should implement a more robust method to enforce a specific parser in
upstream rpmautospec, rather than relying on the existing approach: Using an
environment variable lacks reliability—for instance, there is a risk the
variable may not be propagated correctly through the stack

--- a/rpmautospec/pkg_history.py        2026-03-18 01:00:00.000000000 +0100
+++ b/rpmautospec/pkg_history.py        2026-03-24 09:50:41.657815984 +0100
@@ -12,7 +12,7 @@
 from .changelog import ChangelogEntry
 from .compat import BlobIO, pygit2, rpm
 from .magic_comments import parse_magic_comments
-from .specparser import AutoSpecParser, SpecParserError
+from .specparser import NoRPMSpecParser as AutoSpecParser, SpecParserError

 if TYPE_CHECKING:
     from .specparser import SpecParser

